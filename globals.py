from enum import IntEnum

WIDTH, HEIGHT = 800, 800
# Colors
WHITE = (255, 255, 255)
BLACK = (0, 0, 0)
RED = (255, 0, 0)
GREEN = (0, 255, 0)
BLUE = (0, 0, 255)

# double click to place a point

# single click on a point to select it
# and drag and drop

# empty drag -> span rectangle to select multiple points
# C to compute the convex hull

# single click and shift to define a polygon
# when shift not pressed anymore or missclick -> polygon done

# T when points selected to triangulate

